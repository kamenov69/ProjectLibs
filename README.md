# STM32L010RBLibTest
Tests of some libraries and gid features...

# Връщам се отново към STM32l010RB ...

## Stable starting point

For new projects, start from tag `v0.1.0`:

```bash
git clone --branch v0.1.0 --recurse-submodules <repo-url>
